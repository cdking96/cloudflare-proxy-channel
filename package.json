{
  "name": "channel",
  "version": "1.0.0",
  "description": "Cloudflare Worker for proxying AI API requests",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "itty-router": "^5.0.23"
  },
  "devDependencies": {
    "wrangler": "^4.75.0"
  }
}
